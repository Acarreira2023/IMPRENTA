# Imprenta App

Estructura del proyecto y pasos iniciales.

- Sheets-first: backend Apps Script activo por defecto.
- Firebase preconfigurado en backend/firebase pero deshabilitado.
- Frontend web en /frontend (React + TypeScript + Vite).
- Mobile con Expo en /mobile-expo (TypeScript).
