import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';

// https://vite.dev/config/
export default defineConfig({
  plugins: [
    react(),
    tailwindcss(), // Activa el compilador nativo de Tailwind v4 en Vite 8
  ],
  server: {
    // Se fuerza a que si hay algún error visual de conexión (HMR),
    // intente refrescar la pantalla en lugar de congelarse en negro
    hmr: {
      overlay: true,
    },
  },
});